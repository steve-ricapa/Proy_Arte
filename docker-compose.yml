services:
  backend:
    build:
      context: ./backend
    container_name: proyectoarte-backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: proyectoarte-frontend
    depends_on:
      - backend
    ports:
      - "80:80"
    restart: unless-stopped
